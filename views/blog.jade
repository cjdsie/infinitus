extends layout

block content
  main.main(role='main')
    .container
      - var posts = getPosts(0,5)
      - var page  = 1
      - each post in posts
        include includes/postSnippet
      
      include includes/pagination